<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>BLACK NEW RABBIT | 停止コード入力</title>

  <style>
    :root {
      --bg: #090b13;
      --panel: #151927;
      --panel-light: #20263a;
      --red: #ff365f;
      --pink: #ff75a0;
      --blue: #71d9ff;
      --text: #f5f7ff;
      --sub: #aab1c7;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background:
        radial-gradient(circle at 20% 10%, rgba(255, 54, 95, 0.18), transparent 30%),
        radial-gradient(circle at 80% 90%, rgba(113, 217, 255, 0.12), transparent 30%),
        var(--bg);
      color: var(--text);
      font-family:
        -apple-system, BlinkMacSystemFont, "Noto Sans JP",
        "Hiragino Kaku Gothic ProN", "Yu Gothic", sans-serif;
    }

    .container {
      width: min(92%, 520px);
      margin: 0 auto;
      padding: 28px 0 40px;
    }

    .top-label {
      color: var(--red);
      font-size: 12px;
      font-weight: bold;
      letter-spacing: 0.18em;
      text-align: center;
      margin-bottom: 12px;
    }

    h1 {
      margin: 0;
      text-align: center;
      font-size: clamp(26px, 8vw, 42px);
      letter-spacing: 0.08em;
      text-shadow:
        0 0 8px rgba(255, 54, 95, 0.8),
        0 0 24px rgba(255, 54, 95, 0.35);
    }

    .subtitle {
      text-align: center;
      color: var(--blue);
      font-size: 13px;
      letter-spacing: 0.12em;
      margin-top: 8px;
    }

    .rabbit {
      width: 112px;
      height: 112px;
      margin: 26px auto 20px;
      border: 2px solid var(--red);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 55px;
      background: rgba(255, 54, 95, 0.08);
      box-shadow:
        0 0 15px rgba(255, 54, 95, 0.55),
        inset 0 0 20px rgba(255, 54, 95, 0.12);
    }

    .panel {
      background: rgba(21, 25, 39, 0.94);
      border: 1px solid rgba(113, 217, 255, 0.25);
      border-radius: 16px;
      padding: 22px;
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.28);
    }

    .warning {
      border-left: 4px solid var(--red);
      color: #ffd8e1;
      background: rgba(255, 54, 95, 0.08);
      padding: 13px 14px;
      margin-bottom: 22px;
      line-height: 1.7;
      font-size: 14px;
    }

    .warning strong {
      color: var(--red);
    }

    .description {
      color: var(--sub);
      font-size: 14px;
      line-height: 1.8;
      margin-bottom: 22px;
    }

    label {
      display: block;
      color: var(--blue);
      font-size: 13px;
      font-weight: bold;
      margin: 17px 0 7px;
    }

    input {
      width: 100%;
      min-height: 48px;
      padding: 12px 14px;
      border: 1px solid #414a68;
      border-radius: 9px;
      color: var(--text);
      background: #0c0f1b;
      font-size: 16px;
      outline: none;
    }

    input:focus {
      border-color: var(--blue);
      box-shadow: 0 0 0 3px rgba(113, 217, 255, 0.13);
    }

    #code {
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    button {
      width: 100%;
      min-height: 52px;
      margin-top: 24px;
      border: 0;
      border-radius: 9px;
      color: white;
      background: linear-gradient(135deg, #ff365f, #b71855);
      font-size: 16px;
      font-weight: bold;
      letter-spacing: 0.08em;
      cursor: pointer;
      box-shadow: 0 7px 18px rgba(255, 54, 95, 0.25);
    }

    button:active {
      transform: translateY(1px);
    }

    .message {
      display: none;
      margin-top: 18px;
      padding: 13px;
      border-radius: 8px;
      text-align: center;
      line-height: 1.6;
      font-size: 14px;
    }

    .error {
      display: block;
      color: #ffd7df;
      background: rgba(255, 54, 95, 0.13);
      border: 1px solid rgba(255, 54, 95, 0.4);
    }

    .success-screen {
      display: none;
      text-align: center;
    }

    .success-screen.show {
      display: block;
    }

    .clear-mark {
      color: var(--blue);
      font-size: 50px;
      margin: 8px 0;
      text-shadow: 0 0 18px rgba(113, 217, 255, 0.7);
    }

    .success-screen h2 {
      color: var(--blue);
      font-size: 25px;
      letter-spacing: 0.08em;
      margin: 8px 0 18px;
    }

    .success-text {
      line-height: 1.9;
      font-size: 15px;
    }

    .nickname-result {
      color: var(--pink);
      font-weight: bold;
    }

    .credits {
      margin-top: 28px;
      padding-top: 20px;
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      color: var(--sub);
      font-size: 13px;
      line-height: 1.9;
    }

    .credits-title {
      color: var(--text);
      font-weight: bold;
      letter-spacing: 0.1em;
      margin-bottom: 6px;
    }

    .footer {
      color: #68718b;
      font-size: 11px;
      text-align: center;
      margin-top: 24px;
      line-height: 1.7;
    }
  </style>
</head>

<body>
  <main class="container">

    <div id="inputScreen">
      <div class="top-label">EMERGENCY SHUTDOWN SYSTEM</div>

      <h1>BLACK NEW RABBIT</h1>
      <div class="subtitle">ブラックニューラビート停止システム</div>

      <div class="rabbit">🐇</div>

      <section class="panel">
        <div class="warning">
          <strong>WARNING</strong><br>
          想定外のニューラビート個体を検知しました。<br>
          ブラックニューラビートの活動を停止するには、最終問題から導き出された停止コードが必要です。
        </div>

        <div class="description">
          あなたの名前と停止コードを入力してください。<br>
          正しいコードが確認されると、ブラックニューラビートの停止処理が開始されます。
        </div>

        <form id="codeForm">
          <label for="nickname">ニックネーム</label>
          <input
            id="nickname"
            name="nickname"
            type="text"
            maxlength="20"
            placeholder="例：ゆき"
            autocomplete="nickname"
            required
          >

          <label for="code">停止コード</label>
          <input
            id="code"
            name="code"
            type="text"
            maxlength="50"
            placeholder="停止コードを入力"
            autocomplete="off"
            required
          >

          <button type="submit">停止コードを送信する</button>
        </form>

        <div id="errorMessage" class="message"></div>
      </section>

      <div class="footer">
        ※停止コードは、最終問題から導き出した文字列を入力してください。
      </div>
    </div>

    <section id="successScreen" class="panel success-screen">
      <div class="clear-mark">✓</div>
      <h2>停止処理 成功</h2>

      <div class="success-text">
        <span id="resultNickname" class="nickname-result"></span> さん<br><br>
        ブラックニューラビートの活動停止を確認しました。<br>
        あなたの活躍により、危機は回避されました。
      </div>

      <div class="credits">
        <div class="credits-title">CREDITS</div>
        謎解き企画：〇〇〇〇<br>
        ストーリー：〇〇〇〇<br>
        Web制作：〇〇〇〇<br>
        Special Thanks：納涼祭運営チーム
      </div>
    </section>

  </main>

  <script>
    /*
     * =====================================================
     * 設定項目
     * =====================================================
     *
     * 停止コードが決まったら、下の文字列を変更してください。
     *
     * 例：
     * const STOP_CODE = "HANABI2025";
     *
     * 大文字・小文字は区別しない設定です。
     */
    const STOP_CODE = "ここに停止コードを設定";

    const form = document.getElementById("codeForm");
    const nicknameInput = document.getElementById("nickname");
    const codeInput = document.getElementById("code");
    const errorMessage = document.getElementById("errorMessage");

    const inputScreen = document.getElementById("inputScreen");
    const successScreen = document.getElementById("successScreen");
    const resultNickname = document.getElementById("resultNickname");

    form.addEventListener("submit", function(event) {
      event.preventDefault();

      const nickname = nicknameInput.value.trim();
      const enteredCode = codeInput.value.trim();

      if (nickname === "") {
        showError("ニックネームを入力してください。");
        return;
      }

      if (enteredCode === "") {
        showError("停止コードを入力してください。");
        return;
      }

      /*
       * 前後の空白を除去し、大文字・小文字を区別しない判定にしています。
       */
      const correct =
        enteredCode.toUpperCase() === STOP_CODE.trim().toUpperCase();

      if (correct) {
        resultNickname.textContent = nickname;
        inputScreen.style.display = "none";
        successScreen.classList.add("show");
        window.scrollTo(0, 0);
      } else {
        showError("停止コードが一致しません。最終問題をもう一度確認してください。");
        codeInput.value = "";
        codeInput.focus();
      }
    });

    function showError(message) {
      errorMessage.textContent = message;
      errorMessage.className = "message error";
    }
  </script>
</body>
</html>
